#include <stdio.h>
#include "heap.h"

int main() {
//    heap_setup();
//    void *ptr = heap_realloc(NULL, 179);
//    get_pointer_type(ptr);
//    heap_clean();
}

//  TESTING ENUM POINTER POSITION
//enum pointer_type_t test;
//void* testing = heap_malloc(200);
//void* fence = (unsigned char*)testing - FENCE + 2;
//void* control = (unsigned char*)testing - sizeof(struct memory_chunk_t) + 2;
//void* inside_data = (unsigned char*)testing + 18;
//void* fence2 = (unsigned char*)testing + 200 + FENCE - 2;
//test = get_pointer_type(testing);
//test = get_pointer_type(fence);
//test = get_pointer_type(control);
//test = get_pointer_type(inside_data);
//test = get_pointer_type(fence2);


//  TEST FORM TESTS

